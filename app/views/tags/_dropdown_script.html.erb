<% @group_tags_array = current_group.tags.map {|tag| tag.name} %>

<script>
  $(document).ready(function() {
    $("#e12").select2({tags: <%= raw @group_tags_array.to_json %>}, { width: '400px' });
    });
</script>
