<li id="note_<%= note.id %>">
  <%= link_to image_tag("iconmonstr-trash-can-15-icon.png", :class => 'trash-can'), contact_note_path(@contact, note), method: :delete, remote: true %>
  <%= note.content %> <%= note.created_at.strftime("(%b %e)") %>
</li>
